const Silo = require('./models/Silo');

const swm = new Silo();
console.log(swm);
swm.upgrade();
console.log(swm);
swm.upgrade();
console.log(swm);
swm.upgrade();
console.log(swm);
swm.upgrade();
console.log(swm);